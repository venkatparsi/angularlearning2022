import { AppLanguagePipe } from './app-language.pipe';

describe('AppLanguagePipe', () => {
  it('create an instance', () => {
    const pipe = new AppLanguagePipe();
    expect(pipe).toBeTruthy();
  });
});
