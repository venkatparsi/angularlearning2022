import { environment } from "src/environments/environment";

'use strict';
export const appBaseUrl = environment.baseURL;

export const ctldurl = appBaseUrl + 'ctld/api';
export const ltldurl = appBaseUrl + 'ltld/api';
export const ntldurl = appBaseUrl + 'ntld/api';
export const mtldurl = appBaseUrl + 'mtld/api';
export const rtldurl = appBaseUrl + 'rtld/api';
export const ptldurl = appBaseUrl + 'ptld/api';